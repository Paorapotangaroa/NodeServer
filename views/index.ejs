<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Student List</title>
    <style>
        /* General Reset */
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        /* Background and body styles */
        
        body {
            font-family: 'Arial', sans-serif;
            background-color: #1c1c1c;
            /* Dark background */
            color: #f1f1f1;
            /* Light text */
            padding: 20px;
        }
        
        h1 {
            color: #f1c40f;
            text-align: center;
            font-size: 36px;
            font-weight: bold;
            text-shadow: 2px 2px 5px #000;
            margin-bottom: 30px;
        }
        
        table {
            width: 100%;
            margin: 0 auto;
            border-collapse: collapse;
        }
        /* Style for the list (students) */
        
        li {
            font-size: 20px;
            padding: 10px;
            margin: 10px 0;
            border: 2px solid #f1c40f;
            background-color: #2c3e50;
            /* Dark gray background for each row */
            border-radius: 8px;
            transition: background-color 0.3s ease;
        }
        
        li:hover {
            background-color: #34495e;
            /* Darker gray on hover */
            cursor: pointer;
        }
        /* Special Batman title style for list items */
        
        li::before {
            font-size: 24px;
            margin-right: 10px;
        }
        /* Adding some padding and center aligning the list */
        
        ul {
            list-style-type: none;
            padding: 0;
        }
    </style>
</head>

<body>
    <h1>Students</h1>

    <ul>
        <% for(let i = 0; i<listOfStudents.length; i++) { %>
            <li>
                <%= listOfStudents[i].name %>
            </li>
            <% } %>
    </ul>
</body>

</html>